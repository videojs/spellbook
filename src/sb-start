#!/usr/bin/env node
var Run = require('./utils/run');
var CommanderWrapper = require('./utils/commander-wrapper');

var program = CommanderWrapper(function(commander) {
  return commander;
});

setTimeout(function() {
  Run.one(['sb-server'], {nonFatal: true});
}, 8000);

Run.one(['sb-watch'], {nonFatal: true});
